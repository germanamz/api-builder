version: "3.9"
services:
<% artifactNames.forEach((artifactName) => { %>
    <%- artifactName %>:
        build:
            context: <%- join(outdir, artifactName) %>
            dockerfile: Dockerfile
            args:
                CODEARTIFACT_AUTH_TOKEN=${CODEARTIFACT_AUTH_TOKEN}
<% }) %>
